import { lazy } from 'react'
import type { ProblemDefinition } from '../types'
export const decodeStringProblem={slug:'decode-string',number:'40',leetcodeId:'LeetCode #394',title:'字符串解码',difficulty:'medium',difficultyLabel:'中等',tags:['栈','递归','字符串'],languages:['javascript'],url:'https://leetcode.cn/problems/decode-string/',lead:<>解码形如 <code>k[encoded_string]</code> 的字符串，其中括号内片段需要重复正整数 <code>k</code> 次。</>,note:'遇到字母直接拼接；遇到数字就用 balance 寻找与左括号匹配的右括号，递归解码内部与剩余字符串，再把结果组合。',example:<><code>s = "3[a]2[bc]"</code><strong>→ "aaabcbc"</strong></>,Visualizer:lazy(()=>import('./DecodeStringVisualizer').then(m=>({default:m.DecodeStringVisualizer})))} satisfies ProblemDefinition
