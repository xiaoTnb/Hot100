import { lazy } from 'react'
import type { ProblemDefinition } from '../types'
export const topKFrequentProblem={slug:'top-k-frequent-elements',number:'43',leetcodeId:'LeetCode #347',title:'前 K 个高频元素',difficulty:'medium',difficultyLabel:'中等',tags:['哈希表','桶排序'],languages:['javascript'],url:'https://leetcode.cn/problems/top-k-frequent-elements/',lead:<>给定整数数组和整数 <code>k</code>，返回其中出现频率最高的 k 个元素，顺序任意。</>,note:'Map 先统计每个元素的频率，再以“频率”为桶下标。从最高频率向下遍历桶，取满 k 个元素即可。',example:<><code>nums = [1,1,1,2,2,3]，k = 2</code><strong>→ [1, 2]</strong></>,Visualizer:lazy(()=>import('./TopKFrequentVisualizer').then(m=>({default:m.TopKFrequentVisualizer})))} satisfies ProblemDefinition
